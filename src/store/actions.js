import * as types from './mutation-types'

// export const addInfoLists = function ({commit, state}, {obj}) {
//   let infoLists = state.infoLists.slice()
//   infoLists.push(obj)
//   commit(types.SET_INFOLISTS, infoLists)
// }
